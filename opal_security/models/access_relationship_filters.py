# coding: utf-8

"""
    Opal API

    The Opal API is a RESTful API that allows you to interact with the Opal Security platform programmatically.

    The version of the OpenAPI document: 1.0
    Contact: hello@opal.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from opal_security.models.access_entity_filters import AccessEntityFilters
from typing import Optional, Set
from typing_extensions import Self

class AccessRelationshipFilters(BaseModel):
    """
    Filters the returned nodes by the access edges connected to them. When `isAccessibleBy` and `hasAccessTo` are provided, the returned nodes must satisfy both edge constraints simultaneously. 
    """ # noqa: E501
    is_accessible_by: Optional[AccessEntityFilters] = Field(default=None, description="Inbound-edge filter. The returned node must be accessible by at least one entity matching this filter.", alias="isAccessibleBy")
    has_access_to: Optional[AccessEntityFilters] = Field(default=None, description="Outbound-edge filter. The returned node must have access to at least one entity matching this filter.", alias="hasAccessTo")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["isAccessibleBy", "hasAccessTo"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AccessRelationshipFilters from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of is_accessible_by
        if self.is_accessible_by:
            _dict['isAccessibleBy'] = self.is_accessible_by.to_dict()
        # override the default output from pydantic by calling `to_dict()` of has_access_to
        if self.has_access_to:
            _dict['hasAccessTo'] = self.has_access_to.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AccessRelationshipFilters from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "isAccessibleBy": AccessEntityFilters.from_dict(obj["isAccessibleBy"]) if obj.get("isAccessibleBy") is not None else None,
            "hasAccessTo": AccessEntityFilters.from_dict(obj["hasAccessTo"]) if obj.get("hasAccessTo") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


